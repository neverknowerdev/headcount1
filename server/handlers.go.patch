--- server/handlers.go
+++ server/handlers.go
@@ -37,6 +37,9 @@
		r.Delete("/{id}", s.deleteCompany)
	})

+	registerSettingsRoutes(r, s.db)
+	registerActivityRoutes(r, s.db)
+
	r.Route("/projects", func(r chi.Router) {
		r.Get("/", s.listProjects)
		r.Post("/", s.createProject)
