--- server/server.go
+++ server/server.go
@@ -37,6 +37,8 @@
	registerAgentRoutes(r, s.db)
	registerProviderRoutes(r, s.db)
	registerSkillRoutes(r, s.db)
+	registerSettingsRoutes(r, s.db)
+	registerActivityRoutes(r, s.db)

	r.HandleFunc("/ws", s.handleWebSocket)
 }
